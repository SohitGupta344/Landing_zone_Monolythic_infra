trigger:
- main

pool: default

stages:
  - stage: 
    jobs:
      - job: Scanning
        steps:
        - task: PowerShell@2
          inputs:
            targetType: 'inline'
            script: |
              cd  cd C:\agent\_work
              cd env/dev