trigger:
- main

pool: default

stages:
  - stage: 
    jobs:
      - job: Scanning
        steps:
        - task: PowerShell@2
          inputs:
            targetType: 'inline'
            script: |
              cd env/dev
              terraform init